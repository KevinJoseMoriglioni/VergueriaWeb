<div class="row m-0">

    <div class="col-12 col-center p-0">

        {{-- <form action="/guardar" method="POST">

            <div class="form-group row m-0 text-center">
                <div class="p-2">
                    <button type="submit" class="btn bg-vergueria">Enviar</button>
                </div>
            </div>

        </form> --}}

        {{-- {{Form::open(array('url' => 'wta'))}}
        <div>
            {{Form::label('Nombre', 'Nombre')}}
            {{Form::text("Nombre")}}
        </div>
        <div>
            {{Form::select('size', array('L' => 'Large', 'S' => 'Small'));}}
        </div>
        {{Form::close()}} --}}
    </div>

</div>